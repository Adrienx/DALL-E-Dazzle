import DataContext from "../DataContext"
import React, { useContext } from "react"

export default function FavoriteImages() {
  //   const { userInfo, setUserInfo } = useContext(DataContext)
  return (
    <div className="card">
      <h2>Fav Images Page</h2>
    </div>
  )
}
