import DataContext from "../DataContext"
import React, { useContext } from "react"

export default function BrowseImages() {
  //   const { userInfo, setUserInfo } = useContext(DataContext)
  return (
    <div className="card">
      <h2>Browse Images Page</h2>
    </div>
  )
}
