import DataContext from "../DataContext"
import React, { useContext } from "react"

export default function CreatePrompt() {
  //   const { userInfo, setUserInfo } = useContext(DataContext)
  return (
    <div className="card">
      <h2>Generate Prompt Page</h2>
    </div>
  )
}
